# SQL_Work
This is all of my SQL work in my database management class. It features multiple basic queries and advanced queries. It uses databases that are run through the University of Akron's server. The advanced queries reach topics such as subqueries, tuples, recursive queries, hierarchical queries, contains, and match similarity.
